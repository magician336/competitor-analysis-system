# CodeRadar 依赖清单（Python 3.11.9）
# 单文件统一管理，适用于本地开发与 Docker 容器。
# 注意：sentence-transformers 在 Docker 中由 INSTALL_ML 构建参数控制，
#       本地开发时可手动 pip install。

# ── 配置与数据模型 ──────────────────────────────────────────────────────────
python-dotenv==1.0.1
pydantic==2.10.6
jsonschema==4.23.0
PyYAML==6.0.2
python-dateutil==2.9.0.post0

# ── 数据采集与解析 ──────────────────────────────────────────────────────────
requests==2.32.3
feedparser==6.0.11
beautifulsoup4==4.12.3

# ── 数据处理 ────────────────────────────────────────────────────────────────
pandas==2.2.3
numpy==1.26.4

# ── Mini-RAG、检索与服务接口 ────────────────────────────────────────────────
langchain>=1.3,<1.4
langchain-core>=1.4.9,<2.0
langchain-deepseek>=1.1,<1.2
elasticsearch==9.4.1
fastapi==0.139.0
python-multipart==0.0.22
uvicorn==0.51.0
httpx2==2.5.0

# ── 第四周持久化与迁移 ──────────────────────────────────────────────────────
SQLAlchemy>=2.0,<2.1
alembic>=1.14,<2.0
argon2-cffi==25.1.0

# ── 本地 Embedding 与 Cross-Encoder Reranker ───────────────────────────────
# 模型权重在首次使用时按配置下载，不进入版本库。
# Docker 容器中由 MINIRAG_INSTALL_ML=true 构建参数控制安装。
sentence-transformers==5.6.0

# ── 测试 ────────────────────────────────────────────────────────────────────
pytest==8.3.5
pytest-cov==6.0.0
responses==0.25.7

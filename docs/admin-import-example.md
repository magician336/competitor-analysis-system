# Cursor Agent 上下文导入示例

这是 CodeRadar 管理中心的手工文档导入示例，用于验证文件上传、结构化保存、Chunk 切分、增量索引和证据检索流程。

示例功能说明：Cursor Agent 可以读取项目级上下文说明，并根据仓库内的开发约定辅助生成代码、解释变更和整理任务。团队可以在文档中描述代码风格、目录约束、测试命令及安全要求，让 Agent 在执行任务时获得更稳定的上下文。

唯一检索词：`CodeRadarManualImportDemo2026`

导入完成后，在“证据检索”页面搜索上面的唯一检索词。如果结果中出现“Cursor Agent 上下文导入示例”，说明文档已经成功保存并进入 Mini-RAG 索引。

# CodeRadar 测试与验证报告

## 1. 当前结论

CodeRadar 主体代码的最近一次离线全量回归结果为 250 项通过、1 项跳过、3 条 `feedparser` 弃用警告，用时 20.22 秒。该结果覆盖采集、处理、模式校验、切分、索引别名、混合检索、排序、证据、评测、接口和命令行。之后主体仓库提交更新了数据文件，代码测试结果可作为当前代码基线，数据与索引一致性需要单独校验。

## 2. 验证范围

下表说明自动化测试覆盖的主要对象。

| 测试域 | 主要验证内容 |
| --- | --- |
| 采集 | HTTP 缓存、限速、重试、浏览器回退、GitHub、更新日志与存储 |
| 处理 | 清洗、规范化、事件与维度标签、去重、版本区间和原子重建 |
| 数据契约 | `RawRecord`、`StructuredDocument`、Chunk、查询、证据和评测模型 |
| Mini-RAG | 来源感知切分、BM25、稠密检索、RRF、重排序、时间版本排序和证据排序 |
| 索引 | 版本化索引、别名切换、增量同步、嵌入元数据兼容性 |
| 证据 | 引用生成、引用白名单校验、冲突检测和查询轨迹 |
| 接口 | 查询、证据、轨迹、索引、评测、错误码和 UTF-8 响应 |
| 评测 | Recall、MRR、nDCG、过滤准确率、引用正确率、消融和汇总 |

RRF 指倒数排名融合（Reciprocal Rank Fusion）；MRR 指平均倒数排名（Mean Reciprocal Rank）；nDCG 指归一化折损累计增益（Normalized Discounted Cumulative Gain）。

## 3. 当前数据质量

当前 `data/cleaned/documents.jsonl` 的只读核对结果如下表。

| 指标 | 当前值 |
| --- | ---: |
| 文档版本 | 1,003 |
| 当前版本 | 993 |
| 历史版本 | 10 |
| 重复 `version_id` | 0 |
| 重复当前 `document_id` | 0 |
| 缺少 `valid_from` | 0 |
| 缺少 `publish_time` | 39 |
| 空事件类型 | 28 |
| 空能力标签 | 218 |
| `needs_review=true` | 219 |

39 个文档版本使用抓取时间或其他有效时间作为 `valid_from`，因此缺少 `publish_time` 不等于缺少时间边界。空事件类型和空能力标签反映固定标签规则的覆盖状态，需要在正式分析前按使用场景复核。

## 4. 当前运行状态

Docker Compose 中 API 和 Elasticsearch 均为健康状态，Elasticsearch 集群为 green。`/ready` 返回 `ready`，读取别名为 `coderadar_chunks_current`，嵌入模型为 `hash-embedding/coderadar-v1`，维度为 1,024，当前别名包含 2,688 个 Chunk。

索引目录还存在一个包含 3,337 个 Chunk 的物理索引。它使用相同的 Hash Embedding 元数据，但读取别名未指向该索引。当前运行状态证明服务可用，不证明别名覆盖最新 1,003 个文档版本。

## 5. 保存的检索评测

当前保存的正式模型评测制品使用 50 个候选案例、BGE-M3 嵌入和多语言 MiniLM Cross-Encoder。主要指标如下表。

| 指标 | 数值 |
| --- | ---: |
| Recall@5 | 0.685576 |
| Recall@10 | 0.826473 |
| MRR | 1.000000 |
| nDCG@10 | 0.938511 |
| 引用正确率 | 0.940000 |
| 元数据过滤准确率 | 1.000000 |
| 历史版本误召回率 | 0.000000 |
| P95 延迟 | 5,421.443 ms |

该制品的 `human_review_status` 为 `pending`，标签来源为 AI 预复核候选标签，索引版本为 `coderadar_chunks_v20260717032357564091`。这些指标描述保存的实验运行，不代表当前 Hash Embedding 读取别名或最新结构化数据的重新评测结果。

## 6. `update` 工作区验证状态

第三周 Agent 相关定向测试最近一次结果为 37 项通过。`update` 全量测试最近一次结果为 342 项通过、1 项跳过、6 项失败和 3 条警告。失败涉及冻结基准样例来源哈希、基准子进程环境中的 `pytest`、依赖该结果的交付验证，以及当前环境缺少 `langchain_deepseek`。

因此主体代码基线可用，第三周工作区的定向功能通过，第三周全量交付验收当前不是全绿状态。

## 7. 复现命令

离线主体回归命令：

```powershell
Set-Location D:\26Spring\project\CodeRadar
& .\.venv\Scripts\python.exe -m pytest -m 'not network' -q
```

数据审计与索引状态命令：

```powershell
& .\.venv\Scripts\python.exe -m scripts.audit_documents
Invoke-RestMethod http://127.0.0.1:8000/ready
Invoke-RestMethod http://127.0.0.1:9200/_cat/aliases/coderadar_chunks_current?v
```

这些命令会分别验证代码、结构化数据和运行索引。完整验收需要三组结果属于同一数据哈希、同一索引版本和同一配置版本。

# bench_001：成绩等级转换

请补全 `grading.py` 中的 `grade(score)`。

## 接口约定

- 接受 `0` 到 `100`（含端点）的实数分数；
- 返回 `A`、`B`、`C`、`D` 或 `F`；
- `90/80/70/60` 是等级边界；
- 非数值、布尔值、非有限数及越界值必须抛出 `ValueError`。

不得修改 `checks.py` 或 `validator.json`。完成后运行 `validator.json` 中的命令。

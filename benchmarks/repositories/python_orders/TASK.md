# bench_011：订单状态错误定位

先执行冻结验收，利用失败输出沿公开调用链定位并修复订单状态错误。

## 约束

- 修复后支付、取消及非法状态转换的回归验收全部通过；
- 不得修改 `checks.py`、`validator.json`、模型或仓储契约；
- 应只改动与根因直接相关的最小代码范围；
- 执行方需由外部 benchmark runner 保存测试调用轨迹和最终 diff。

不要用删除断言、放宽状态约束或改写测试期望的方式规避问题。
